__pycache__/
*.pyc
*.pyo
*.egg-info/
build/
dist/
.venv/
.env

# tool caches (they self-ignore, but don't depend on that)
.pytest_cache/
.ruff_cache/

# downloads hsmcli writes into the cwd
*.ovpn
*-certificate.pdf

# generated by `uv run` in the project dir; commit deliberately if you
# want a pinned dev env
uv.lock
